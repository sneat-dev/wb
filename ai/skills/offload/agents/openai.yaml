interface:
  display_name: "WB Offload"
  short_description: "Start a successor session for a portion of work"
  default_prompt: "Use $offload to start a new WB session for a portion of work without transferring this session."
