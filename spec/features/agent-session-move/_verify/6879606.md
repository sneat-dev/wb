```yaml
feature: agent-session-move
revision: 6879606
verdicts:
  - ac: agent-session-move#ac:ssh-move-starts-same-harness-successor
    verdict: pass
    justification: "The commit fetches and verifies the exact pushed tip into a deterministic isolated worktree, preserves the source runtime when no harness override is given, and idempotently starts one detached tmux successor whose fixed harness prompt names the handover path."
  - ac: agent-session-move#ac:explicit-cross-harness-move
    verdict: pass
    justification: "Explicit --harness is preserved in the portable request; target launch selects the different installed harness, registers a fresh successor, and passes only the immutable handover path—not a source transcript."
  - ac: agent-session-move#ac:unpublished-work-refuses-before-delivery
    verdict: pass
    justification: "All four refusal cases abort with explicit errors before checkpoint mutation; focused exact-commit tests pass, confirming unchanged Git/Work Log state, while the command has no delivery or target-start path."
  - ac: agent-session-move#ac:receipt-completes-custody-and-lineage
    verdict: pass
    justification: "Receipt follows verified live registration; source acknowledgement durably stores identity/lineage before sealing predecessor custody as external_handoff, and both Work Logs carry the same handoff ID."
  - ac: agent-session-move#ac:failed-or-retried-delivery-is-idempotent
    verdict: fail
    justification: "The commit replays a stored receipt, but implements no receiver that prevents duplicate worktree or tmux creation; session records are keyed only by PID, so exactly one successor session is not guaranteed."
  - ac: agent-session-move#ac:moved-branch-refuses-at-target
    verdict: pass
    justification: "The live branch tip is compared to the pinned bundle commit before target-worktree creation; mismatch returns an error, the receive layer appends a durable failed event without a receipt or successor startup, leaving receipt-gated source custody unchanged."
  - ac: agent-session-move#ac:synchestra-uses-the-same-receiver-contract
    verdict: pass
    justification: "The adapter sends canonical request bytes unchanged to fixed handler wb.session.accept.v1 and validates its receipt and lineage through the same shared contract used by SSH."
  - ac: agent-session-move#ac:successor-can-be-messaged-and-asked-back
    verdict: pass
    justification: "Text and request_handoff messages are persisted before delivery, pasted as exact bytes into the verified live successor pane, acknowledged by lineage-bound receipts, and preserve the predecessor as reply target; focused tests pass."
```

## AC: ssh-move-starts-same-harness-successor

**Verdict:** pass

**Justification:** The commit fetches and verifies the exact pushed tip into a deterministic isolated worktree, preserves the source runtime when no harness override is given, and idempotently starts one detached tmux successor whose fixed harness prompt names the handover path.

**Commits:**

- 1ed0936e7e9555a5cbfcd2785948a017068b9edd — feat(session): launch successors through SSH

**Evidence:**

- `internal/worktrees/session_receive.go:176`
- `internal/sessionlaunch/harness.go:36`
- `internal/sessionlaunch/launch.go:127`
- `internal/sessionlaunch/tmux.go:19`
- `internal/sessionlaunch/launch_test.go:45`

## AC: explicit-cross-harness-move

**Verdict:** pass

**Justification:** Explicit `--harness` is preserved in the portable request; target launch selects the different installed harness, registers a fresh successor, and passes only the immutable handover path—not a source transcript.

**Commits:**

- 1ed0936e7e9555a5cbfcd2785948a017068b9edd — feat(session): launch successors through SSH

**Evidence:**

- `cmd/wb/session_move.go`
- `internal/sessionlaunch/harness.go`
- `internal/sessionlaunch/private.go`
- `internal/sessionlaunch/harness_test.go`

## AC: unpublished-work-refuses-before-delivery

**Verdict:** pass

**Justification:** All four refusal cases abort with explicit errors before checkpoint mutation; focused exact-commit tests pass, confirming unchanged Git/Work Log state, while the command has no delivery or target-start path.

**Commits:**

- 2bb32a545575b5f81cc3aa9d53f2cc706f2844e8 — feat(session): add exact source handover checkpoint

**Evidence:**

- `internal/worktrees/session_checkpoint.go`
- `internal/worktrees/session_checkpoint_test.go:TestCreateSessionCheckpointRefusesBeforeAnyMutation`
- `cmd/wb/session_move.go`
- `cmd/wb/session_move_test.go:TestSessionMoveCommandRefusesMissingSessionAndEmptyHandoverBeforeCheckpoint`

## AC: receipt-completes-custody-and-lineage

**Verdict:** pass

**Justification:** Receipt follows verified live registration; source acknowledgement durably stores identity/lineage before sealing predecessor custody as external_handoff, and both Work Logs carry the same handoff ID.

**Commits:**

- 68c9c29b9052ef8c2d8602ac0ce6be32b6188652 — feat(session): seal receipt-gated custody lineage

**Evidence:**

- `internal/sessionlaunch/launch.go:677`
- `internal/sessionreceive/receive.go:296`
- `internal/sessioncustody/acknowledge.go:53`
- `internal/worktrees/session_custody.go:29`
- `internal/sessioncustody/acknowledge_test.go:17`
- `internal/worktrees/session_custody_test.go:407`

## AC: failed-or-retried-delivery-is-idempotent

**Verdict:** fail

**Justification:** The commit replays a stored receipt, but implements no receiver that prevents duplicate worktree or tmux creation; session records are keyed only by PID, so exactly one successor session is not guaranteed.

**Commits:**

- 7e751bc1d32b685d0e8aa4b74e4273dd8057b56c — feat(session): add durable session move identity and state

**Evidence:**

- `internal/sessionmove/store.go:37`
- `internal/sessionmove/store_test.go:47`
- `internal/session/session.go:72`

## AC: moved-branch-refuses-at-target

**Verdict:** pass

**Justification:** The live branch tip is compared to the pinned bundle commit before target-worktree creation; mismatch returns an error, the receive layer appends a durable failed event without a receipt or successor startup, leaving receipt-gated source custody unchanged.

**Commits:**

- ff455808347acf1de196af473e8bde9839c15f4c — feat(session): receive exact pinned handoff bundle

**Evidence:**

- `internal/worktrees/session_receive.go:91`
- `internal/sessionreceive/receive.go:104`
- `internal/worktrees/session_receive_test.go:365`
- `internal/sessionreceive/receive_test.go:100`

## AC: synchestra-uses-the-same-receiver-contract

**Verdict:** pass

**Justification:** The adapter sends canonical request bytes unchanged to fixed handler `wb.session.accept.v1` and validates its receipt and lineage through the same shared contract used by SSH.

**Commits:**

- 1edc5146b7bc59cfa55b9e460f82e9dd838b33e1 — feat(session): add Synchestra courier adapter

**Evidence:**

- `internal/sessioncourier/synchestra.go`
- `internal/sessioncourier/receiver.go`
- `internal/sessioncourier/ssh.go`
- `internal/sessioncourier/synchestra_test.go`
- `cmd/wb/session_move_test.go`

## AC: successor-can-be-messaged-and-asked-back

**Verdict:** pass

**Justification:** Text and `request_handoff` messages are persisted before delivery, pasted as exact bytes into the verified live successor pane, acknowledged by lineage-bound receipts, and preserve the predecessor as reply target; focused tests pass.

**Commits:**

- 68796061c018f8d2627cda74cf5cbb5e934bcb8c — feat(session): add durable successor messaging

**Evidence:**

- `internal/sessionmessenger/send.go`
- `internal/sessionmessage/receive.go`
- `internal/sessionmove/message.go`
- `cmd/wb/session_message.go`
- `internal/sessionmessenger/send_test.go`
- `internal/sessionmessage/receive_test.go`
