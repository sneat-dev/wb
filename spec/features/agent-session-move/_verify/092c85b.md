```yaml
feature: agent-session-move
revision: 092c85b
verdicts:
  - ac: agent-session-move#ac:ssh-move-starts-same-harness-successor
    verdict: pass
    justification: "The SSH receiver pins an isolated worktree to BundleCommit, launches one detached named tmux session, defaults to SourceRuntime/SourceModel, and embeds HandoverPath in the fixed initial prompt; replay coverage proves no relaunch."
  - ac: agent-session-move#ac:explicit-cross-harness-move
    verdict: pass
    justification: "Explicit --harness is carried in the portable request; target launch selects the different fixed harness, drops the source model, verifies the same immutable handover, and execs a fresh registered tmux successor without transcript conversion or compatibility checks."
  - ac: agent-session-move#ac:unpublished-work-refuses-before-delivery
    verdict: pass
    justification: "The commit rejects empty handovers, dirty trees, detached HEADs, and non-fast-forward pushes before mutation; tests confirm unchanged HEAD/status/Work Log, active source custody, no force push, and no courier or target startup."
  - ac: agent-session-move#ac:receipt-completes-custody-and-lineage
    verdict: pass
    justification: "Commit persists a validated receipt with both WB session IDs, target machine, and tmux identity; embeds the same handoff ID in both Work Logs; and refuses predecessor terminalization until the exact receipt and successor address are durable."
  - ac: agent-session-move#ac:failed-or-retried-delivery-is-idempotent
    verdict: pass
    justification: "The receiver re-admits under a handoff lock and returns the durable receipt before repeating effects; the replay test confirms identical receipts and exactly one worktree, tmux launch, and successor WB session."
  - ac: agent-session-move#ac:moved-branch-refuses-at-target
    verdict: pass
    justification: "The live branch tip is checked before target worktree creation; mismatch returns an error, the receiver appends PhaseFailed without starting a successor or transferring custody, and the source's offer-only Work Log claim remains active."
  - ac: agent-session-move#ac:synchestra-uses-the-same-receiver-contract
    verdict: pass
    justification: "The commit sends unchanged request bytes to fixed handler wb.session.accept.v1, then validates its receipt through the same receiver contract as SSH; focused tests confirm exact bytes and identical receipt/lineage."
  - ac: agent-session-move#ac:successor-can-be-messaged-and-asked-back
    verdict: pass
    justification: "Both text and request_handoff messages are durably persisted, lineage-validated, pasted as exact bytes into the verified live successor pane, and acknowledged by receipts preserving the predecessor reply target."
```

## AC: ssh-move-starts-same-harness-successor

**Verdict:** pass

**Justification:** The SSH receiver pins an isolated worktree to `BundleCommit`, launches one detached named tmux session, defaults to `SourceRuntime`/`SourceModel`, and embeds `HandoverPath` in the fixed initial prompt; replay coverage proves no relaunch.

**Commits:**

- 1ed0936e7e9555a5cbfcd2785948a017068b9edd — feat(session): launch successors through SSH

**Evidence:**

- `internal/worktrees/session_receive.go`
- `internal/sessionlaunch/harness.go`
- `internal/sessionlaunch/tmux.go`
- `internal/sessionlaunch/launch_test.go`

## AC: explicit-cross-harness-move

**Verdict:** pass

**Justification:** Explicit `--harness` is carried in the portable request; target launch selects the different fixed harness, drops the source model, verifies the same immutable handover, and execs a fresh registered tmux successor without transcript conversion or compatibility checks.

**Commits:**

- 1ed0936e7e9555a5cbfcd2785948a017068b9edd — feat(session): launch successors through SSH

**Evidence:**

- `internal/sessionlaunch/harness.go`
- `internal/sessionlaunch/harness_test.go`
- `internal/sessionlaunch/private.go`
- `internal/sessioncourier/ssh_test.go`

## AC: unpublished-work-refuses-before-delivery

**Verdict:** pass

**Justification:** The commit rejects empty handovers, dirty trees, detached HEADs, and non-fast-forward pushes before mutation; tests confirm unchanged HEAD/status/Work Log, active source custody, no force push, and no courier or target startup.

**Commits:**

- 2bb32a545575b5f81cc3aa9d53f2cc706f2844e8 — feat(session): add exact source handover checkpoint

**Evidence:**

- `internal/worktrees/session_checkpoint.go`
- `internal/worktrees/session_checkpoint_test.go`
- `cmd/wb/session_move.go`
- `cmd/wb/session_move_test.go`

## AC: receipt-completes-custody-and-lineage

**Verdict:** pass

**Justification:** The commit persists a validated receipt with both WB session IDs, target machine, and tmux identity; embeds the same handoff ID in both Work Logs; and refuses predecessor terminalization until the exact receipt and successor address are durable.

**Commits:**

- 68c9c29b9052ef8c2d8602ac0ce6be32b6188652 — feat(session): seal receipt-gated custody lineage

**Evidence:**

- `internal/sessionreceive/receive.go`
- `internal/sessioncustody/acknowledge.go`
- `internal/sessionmove/route.go`
- `internal/worktrees/session_custody.go`
- `internal/worktrees/session_custody_test.go`

## AC: failed-or-retried-delivery-is-idempotent

**Verdict:** pass

**Justification:** The receiver re-admits under a handoff lock and returns the durable receipt before repeating effects; the replay test confirms identical receipts and exactly one worktree, tmux launch, and successor WB session.

**Commits:**

- 7e751bc1d32b685d0e8aa4b74e4273dd8057b56c — feat(session): add durable session move identity and state
- 092c85bc0815703abdd39acd3a7cb963fb551622 — test(session): prove receiver replay idempotency

**Evidence:**

- `internal/sessionmove/store.go`
- `internal/sessionreceive/receive.go`
- `internal/sessionreceive/receive_test.go`
- `go test ./internal/sessionreceive -run TestReceiveIdenticalRetryReturnsReceiptWithoutDuplicateReceiverEffects`

## AC: moved-branch-refuses-at-target

**Verdict:** pass

**Justification:** The live branch tip is checked before target worktree creation; mismatch returns an error, the receiver appends `PhaseFailed` without starting a successor or transferring custody, and the source's offer-only Work Log claim remains active.

**Commits:**

- ff455808347acf1de196af473e8bde9839c15f4c — feat(session): receive exact pinned handoff bundle

**Evidence:**

- `internal/worktrees/session_receive.go:91`
- `internal/worktrees/session_receive_test.go:365`
- `internal/sessionreceive/receive.go:100`
- `internal/sessionreceive/receive_test.go:100`
- `internal/worktrees/session_checkpoint_test.go:92`

## AC: synchestra-uses-the-same-receiver-contract

**Verdict:** pass

**Justification:** The commit sends unchanged request bytes to fixed handler `wb.session.accept.v1`, then validates its receipt through the same receiver contract as SSH; focused tests confirm exact bytes and identical receipt/lineage.

**Commits:**

- 1edc5146b7bc59cfa55b9e460f82e9dd838b33e1 — feat(session): add Synchestra courier adapter

**Evidence:**

- `internal/sessioncourier/synchestra.go`
- `internal/sessioncourier/receiver.go`
- `internal/sessioncourier/ssh.go`
- `internal/sessioncourier/synchestra_test.go`
- `cmd/wb/session_move_test.go`

## AC: successor-can-be-messaged-and-asked-back

**Verdict:** pass

**Justification:** Both text and `request_handoff` messages are durably persisted, lineage-validated, pasted as exact bytes into the verified live successor pane, and acknowledged by receipts preserving the predecessor reply target.

**Commits:**

- 68796061c018f8d2627cda74cf5cbb5e934bcb8c — feat(session): add durable successor messaging

**Evidence:**

- `internal/sessionmessenger/send.go`
- `internal/sessionmessage/receive.go`
- `internal/sessionmove/message.go`
- `internal/sessionmessenger/send_test.go`
- `internal/sessionmessage/receive_test.go`
