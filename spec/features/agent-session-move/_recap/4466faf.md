```yaml
feature: agent-session-move
revision: 4466faf
verify_revision: 6879606
drift:
  - ac: agent-session-move#ac:ssh-move-starts-same-harness-successor
    verdict: no-drift
    narrative: "The SSH move path pins the pushed commit in an isolated worktree, defaults to the source harness, includes the handover path in the prompt, and idempotently starts one detached tmux successor."
  - ac: agent-session-move#ac:explicit-cross-harness-move
    verdict: no-drift
    narrative: "The explicit harness is preserved in the immutable request; the target starts a fresh fixed invocation for that harness, passes no source transcript identity, and directs it to the same digest-verified portable handover."
  - ac: agent-session-move#ac:unpublished-work-refuses-before-delivery
    verdict: no-drift
    narrative: "Empty handover, dirty tree, detached HEAD, and non-fast-forward publication all return explicit errors before checkpoint mutation; courier delivery is only invoked after checkpoint success, so no staging, commit, force-push, target start, or source-custody change occurs."
  - ac: agent-session-move#ac:receipt-completes-custody-and-lineage
    verdict: no-drift
    narrative: "Live tmux/WB registration gates receipt creation; the source then durably stores both session IDs, target machine, tmux identity, and shared Work Log lineage before sealing predecessor custody as external_handoff."
  - ac: agent-session-move#ac:failed-or-retried-delivery-is-idempotent
    verdict: spec-tighter-than-code
    narrative: "Durable admission replays an existing receipt, but no receiver gates worktree, tmux, or session creation on replay; session registration is keyed by PID, so duplicate successors for one handoff remain possible."
  - ac: agent-session-move#ac:moved-branch-refuses-at-target
    verdict: no-drift
    narrative: "The receiver rejects any fetched-tip mismatch before target worktree or successor creation, durably appends a failed phase without a receipt, and source custody remains unchanged because sealing requires successful receipt acknowledgement."
  - ac: agent-session-move#ac:synchestra-uses-the-same-receiver-contract
    verdict: no-drift
    narrative: "Synchestra sends unchanged request bytes to fixed handler wb.session.accept.v1 and passes its receipt through the same receipt-and-lineage validator used by SSH."
  - ac: agent-session-move#ac:successor-can-be-messaged-and-asked-back
    verdict: no-drift
    narrative: "Both text and request_handoff messages are addressed by successor WB session ID, durably persisted before transport, pasted as exact bytes into the verified live tmux pane, acknowledged by durable receipts, and bound to the original handoff lineage with the predecessor retained as reply target."
```

## AC: ssh-move-starts-same-harness-successor

**Drift verdict:** no-drift

**Narrative:** The SSH move path pins the pushed commit in an isolated worktree, defaults to the source harness, includes the handover path in the prompt, and idempotently starts one detached tmux successor.

**Verify verdict:** pass

**Verify justification:** The commit fetches and verifies the exact pushed tip into a deterministic isolated worktree, preserves the source runtime when no harness override is given, and idempotently starts one detached tmux successor whose fixed harness prompt names the handover path.

**Commits:**

- 1ed0936e7e9555a5cbfcd2785948a017068b9edd — feat(session): launch successors through SSH

**Evidence:**

- `internal/worktrees/session_receive.go`
- `internal/sessionlaunch/harness.go`
- `internal/sessionlaunch/launch.go`
- `internal/sessionlaunch/tmux.go`
- `cmd/wb/session_move_test.go`

## AC: explicit-cross-harness-move

**Drift verdict:** no-drift

**Narrative:** The explicit harness is preserved in the immutable request; the target starts a fresh fixed invocation for that harness, passes no source transcript identity, and directs it to the same digest-verified portable handover.

**Verify verdict:** pass

**Verify justification:** Explicit --harness is preserved in the portable request; target launch selects the different installed harness, registers a fresh successor, and passes only the immutable handover path—not a source transcript.

**Commits:**

- 1ed0936e7e9555a5cbfcd2785948a017068b9edd — feat(session): launch successors through SSH

**Evidence:**

- `internal/worktrees/session_checkpoint.go`
- `internal/sessionlaunch/harness.go`
- `internal/sessionlaunch/harness_test.go`
- `internal/sessionlaunch/private.go`

## AC: unpublished-work-refuses-before-delivery

**Drift verdict:** no-drift

**Narrative:** Empty handover, dirty tree, detached HEAD, and non-fast-forward publication all return explicit errors before checkpoint mutation; courier delivery is only invoked after checkpoint success, so no staging, commit, force-push, target start, or source-custody change occurs.

**Verify verdict:** pass

**Verify justification:** All four refusal cases abort with explicit errors before checkpoint mutation; focused exact-commit tests pass, confirming unchanged Git/Work Log state, while the command has no delivery or target-start path.

**Commits:**

- 2bb32a545575b5f81cc3aa9d53f2cc706f2844e8 — feat(session): add exact source handover checkpoint

**Evidence:**

- `internal/worktrees/session_checkpoint.go`
- `internal/worktrees/session_checkpoint_test.go:TestCreateSessionCheckpointRefusesBeforeAnyMutation`
- `cmd/wb/session_move.go`
- `cmd/wb/session_move_test.go:TestSessionMoveCommandRefusesMissingSessionAndEmptyHandoverBeforeCheckpoint`

## AC: receipt-completes-custody-and-lineage

**Drift verdict:** no-drift

**Narrative:** Live tmux/WB registration gates receipt creation; the source then durably stores both session IDs, target machine, tmux identity, and shared Work Log lineage before sealing predecessor custody as external_handoff.

**Verify verdict:** pass

**Verify justification:** Receipt follows verified live registration; source acknowledgement durably stores identity/lineage before sealing predecessor custody as external_handoff, and both Work Logs carry the same handoff ID.

**Commits:**

- 68c9c29b9052ef8c2d8602ac0ce6be32b6188652 — feat(session): seal receipt-gated custody lineage

**Evidence:**

- `internal/sessionlaunch/launch.go`
- `internal/sessionreceive/receive.go`
- `internal/sessioncustody/acknowledge.go`
- `internal/sessionmove/route.go`
- `internal/worktrees/session_custody.go`

## AC: failed-or-retried-delivery-is-idempotent

**Drift verdict:** spec-tighter-than-code

**Narrative:** Durable admission replays an existing receipt, but no receiver gates worktree, tmux, or session creation on replay; session registration is keyed by PID, so duplicate successors for one handoff remain possible.

**Verify verdict:** fail

**Verify justification:** The commit replays a stored receipt, but implements no receiver that prevents duplicate worktree or tmux creation; session records are keyed only by PID, so exactly one successor session is not guaranteed.

**Commits:**

- 7e751bc1d32b685d0e8aa4b74e4273dd8057b56c — feat(session): add durable session move identity and state

**Evidence:**

- `internal/sessionmove/store.go`
- `internal/session/session.go`
- `internal/sessionmove/store_test.go`

## AC: moved-branch-refuses-at-target

**Drift verdict:** no-drift

**Narrative:** The receiver rejects any fetched-tip mismatch before target worktree or successor creation, durably appends a failed phase without a receipt, and source custody remains unchanged because sealing requires successful receipt acknowledgement.

**Verify verdict:** pass

**Verify justification:** The live branch tip is compared to the pinned bundle commit before target-worktree creation; mismatch returns an error, the receive layer appends a durable failed event without a receipt or successor startup, leaving receipt-gated source custody unchanged.

**Commits:**

- ff455808347acf1de196af473e8bde9839c15f4c — feat(session): receive exact pinned handoff bundle

**Evidence:**

- `internal/worktrees/session_receive.go`
- `internal/sessionreceive/receive.go`
- `internal/sessionreceive/receive_test.go`
- `internal/sessioncustody/acknowledge.go`
- `_tests/pinned-target-worktree-moved-branch-refuses-at-target.md`

## AC: synchestra-uses-the-same-receiver-contract

**Drift verdict:** no-drift

**Narrative:** Synchestra sends unchanged request bytes to fixed handler `wb.session.accept.v1` and passes its receipt through the same receipt-and-lineage validator used by SSH.

**Verify verdict:** pass

**Verify justification:** The adapter sends canonical request bytes unchanged to fixed handler wb.session.accept.v1 and validates its receipt and lineage through the same shared contract used by SSH.

**Commits:**

- 1edc5146b7bc59cfa55b9e460f82e9dd838b33e1 — feat(session): add Synchestra courier adapter

**Evidence:**

- `internal/sessioncourier/synchestra.go`
- `internal/sessioncourier/receiver.go`
- `internal/sessioncourier/ssh.go`
- `internal/sessioncourier/synchestra_test.go`

## AC: successor-can-be-messaged-and-asked-back

**Drift verdict:** no-drift

**Narrative:** Both text and `request_handoff` messages are addressed by successor WB session ID, durably persisted before transport, pasted as exact bytes into the verified live tmux pane, acknowledged by durable receipts, and bound to the original handoff lineage with the predecessor retained as reply target.

**Verify verdict:** pass

**Verify justification:** Text and request_handoff messages are persisted before delivery, pasted as exact bytes into the verified live successor pane, acknowledged by lineage-bound receipts, and preserve the predecessor as reply target; focused tests pass.

**Commits:**

- 68796061c018f8d2627cda74cf5cbb5e934bcb8c — feat(session): add durable successor messaging

**Evidence:**

- `internal/sessionmessenger/send.go`
- `internal/sessionmessage/receive.go`
- `internal/sessionmove/message.go`
- `internal/sessionmessenger/send_test.go`
- `internal/sessionmessage/receive_test.go`
