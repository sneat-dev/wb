{
  "name": "wb-hub-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "The wb bench dashboard — mounted at /bench by the hosted site and, later, by wb serve.",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "build:fast": "astro build",
    "check": "astro check",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.6",
    "@fontsource-variable/jetbrains-mono": "^5.2.6",
    "@fontsource-variable/space-grotesk": "^5.2.6",
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@astrojs/check": "^0.9.4",
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
