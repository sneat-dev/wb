#!/bin/sh
set -eu

# Replace this with the product-level checks shared by every matching repo.
# Language checks are contributed independently by the detected Go/Node
# profiles, so this template does not need to repeat them.
echo "Run Sneat product checks here"
