# WB session handover

This immutable handover was generated by WB from deliberately supplied source-agent context.

## Identity and checkpoint

- Schema version: "1"
- Handoff ID: "handoff-e4f26b8176bfe83b42503c7f1bfc22e9"
- Successor WB session ID: "wbs-6355f055d7cfaf6630886771df5c6809"
- Predecessor WB session ID: "wbs-8ba801f777ccedf40e570b724e6d5d6d"
- Source machine: "macbook-air"
- Target machine: "hetzner-vm1"
- Source runtime: "codex"
- Source model: "gpt-5.6-sol"
- Source native harness ID: "01a03875-5b92-7d52-bdb9-46d45c65eb2b"
- Repository remote: "git@github.com:sneat-dev/wb.git"
- Branch: "feat/agent-session-move"
- Source work commit: "a36f98076eaa0e2bd027a586c3765f17c10dae2c"
- Requested harness: ""
- Work Log reference: "worklog:projects/wb-20260825T165624.246117000Z/0d5ec2da49ec9f39f51a71a92c5757759ab8e18b43cc67a7ff2252e5c4d07d09"
- Created at: "2026-08-26T11:46:59.203374Z"

## Summary

Portable WB session movement is implemented and deployed for SSH, with Synchestra protocol and host support implemented.

## Validation evidence

WB, Synchestra CLI, Synchestra VM, Synchestra Cloud, and SpecScore test suites passed; Cloud CI and deployment passed.

## Remaining work

Verify successor identity and messaging, then finish Synchestra live exercise after explicit host-pairing approval and a user Hub token.

## Agent-authored handover

# Agent session movement — handover

## Objective

Deliver a working WB-owned session-movement flow from `macbook-air` to `hetzner-vm1`, with SSH and Synchestra couriers. A move persists and pushes the exact branch, commits a handover document, starts the same harness in a named tmux session, records the successor WB session ID, and supports later messages and a typed request to hand off back.

## Architecture decision

- WB owns the portable handoff protocol, Git/worktree custody, Work Log events, lineage, receipts, and CLI UX.
- SSH and Synchestra are interchangeable couriers.
- SpecScore Studio consumes the flow; it does not own transport.
- Same-harness movement is the default. Cross-harness movement is explicit.

## Completed and pushed

- `synchestra-io/wb`, branch `feat/agent-session-move`, verified HEAD `a36f98076eaa0e2bd027a586c3765f17c10dae2c`.
  - Implements `wb session move`, `receive`, `send`, `receive-message`, and `request-handoff`.
  - SSH receiver creates a managed worktree, checks the exact pushed tip, starts Codex/Claude in a deterministic tmux session, records a stable successor WB session ID, transfers Work Log custody only after a durable receipt, and is replay-idempotent.
  - Synchestra uses fixed handlers `wb.session.accept.v1` and `wb.session.message.v1` with the same request/receipt contract.
  - Fresh SpecStudio verification at HEAD: 8 AC pass, 0 fail.
- `synchestra-io/synchestra`, branch `feat/wb-session-transport`, HEAD `8b5e373`.
  - Adds the fixed dispatch invocation used by WB's Synchestra courier.
- `synchestra-io/synchestra-vm`, branch `feat/wb-session-transport`, HEAD `e76d216`.
  - Executes fixed WB handlers idempotently.
  - Uses the saved host registration token directly for dispatch and heartbeat, recovers rotation only after 401 through `/auth/host/token/refresh`, persists the replacement, and calls `/v1/hosts/register`.
- `synchestra-io/synchestra-cloud` PR #4 merged as `0351269869e48670ebb9d5314665802bc94e85c8` and deployed.
  - `/v1/hosts/register` now accepts either the legacy global API key or the active token of the matching host; invalid/mismatched tokens cannot mutate another host.
- `specscore/specscore-cli`, branch `chore/go-1.27.0`, HEAD `b801582`.

## Installed/deployed state

- Local WB: `/Users/alexandertrakhimenok/.local/bin/wb` from `a36f980`.
- Remote WB: `/home/ai/go/bin/wb` from `a36f980`.
- Local Synchestra CLI: `/Users/alexandertrakhimenok/.local/bin/synchestra` from `8b5e373`.
- Remote VM host release: `/home/ai/.local/share/synchestra/releases/wb-session-transport-e76d216`, promoted through `/home/ai/.local/share/synchestra/current`.
- Remote systemd user service `synchestra-vm-host.service` is active with zero restarts.
- Cloud `/version` reports `sha-0351269`.
- WB remote machine and SSH host are both named `hetzner-vm1`; the source is `macbook-air`.

## Validation already passed

- Full `go test ./...` for WB, Synchestra CLI, Synchestra VM, Synchestra Cloud, and SpecScore CLI.
- Focused race tests for WB replay idempotency, VM auth/dispatch, and Cloud host registration.
- Full relevant `go vet`; SpecScore lint passes.
- Cloud PR and main CI passed; Cloud Run deployment completed.

## Remaining live checks

1. Confirm this successor's tmux name, WB session ID, native Codex session ID, worktree, exact branch tip, and predecessor lineage from the receipt/session registry.
2. Receive a text message from the predecessor and a typed `request_handoff`; acknowledge both and report the exact IDs without exposing secrets.
3. Synchestra host pairing is awaiting explicit browser confirmation because it creates a persistent host registration token. The previous stored token is no longer recognized. After approval, restart `synchestra-vm-host.service` and confirm registration plus dispatch claims succeed without backoff.
4. The local Synchestra courier also needs a user Hub bearer token (`SYNCHESTRA_TOKEN` or configured equivalent). No user token was found locally, and the CLI currently references but does not implement `synchestra auth login`. Do not reuse the VM host token as a user token. If no user token is supplied, record this as the sole external credential blocker after proving all code/deployment contracts.

## Safety and provenance

- All implementation changes were made in WB-managed worktrees; canonical main clones were not edited.
- No credentials are embedded in this handover.
- Do not force-push or rewrite any listed branch.
